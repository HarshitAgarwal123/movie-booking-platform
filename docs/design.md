I implemented the show browsing feature of the movie booking platform.

This allows users to view theatres running a selected movie on a given date.

The system is built using Spring Boot and MySQL.

It stores Movie, Theatre and Show details and exposes a REST API to fetch available shows.

This was chosen as a simple read scenario to demonstrate how users can explore available shows before booking.